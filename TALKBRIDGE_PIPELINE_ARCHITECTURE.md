# هيكلية مشروع TalkBridge للترجمة الفورية وخريطة الطريق

توثق هذه الوثيقة الهيكلية الشاملة لمشروع TalkBridge، بنية المجلدات، هندسة الاتصال المستقرة لحل مشاكل التداخل (Race Conditions)، بالإضافة إلى الأفكار والتحسينات المستقبلية المقترحة.

---

## 1. هيكل المشروع (Project Structure)
ينقسم المشروع إلى واجهة أمامية (Frontend) مبنية بـ React + Vite، وخلفية (Backend) مبنية بـ Node.js + Express + Socket.IO.

*   `server.js`: خادم Node.js المركزي، يدير اتصالات Socket.IO، ويتواصل مع واجهات برمجة التطبيقات (Groq للترجمة، ElevenLabs للصوت).
*   `src/views/`: يحتوي على صفحات التطبيق الرئيسية مثل `RoomPage2.tsx` التي تدير واجهة الغرفة النشطة.
*   `src/app-hooks/`: خطاطيف (Hooks) مخصصة تفصل المنطق المعقد عن واجهة المستخدم:
    *   `useWebSpeechRecognition.ts`: إدارة الميكروفون والتقاط الصوت الخام (Web Speech API).
    *   `useSocketRoom.ts`: إدارة الاتصال بالخادم المركزي للترجمة والدردشة.
    *   `usePeerConnection.ts`: إدارة اتصال WebRTC المباشر (P2P) لنقل الكاميرا والصوت الأصلي.
    *   `useRealtimeTranslation.ts`: إدارة حالة معالجة النصوص.
*   `src/state/`: إدارة الحالة العامة للتطبيق باستخدام Zustand (`roomStore.ts`).
*   `src/ui/`: المكونات المرئية المستقلة (Components) ذات التصميم الحديث.

---

## 2. هيكلية الاتصال والترجمة (Pipeline Architecture)
تم فصل المهام إلى مسارين منفصلين لضمان سرعة واستقرار الاتصال:

### المسار الأول: P2P / WebRTC (عبر PeerJS)
*   **المهمة:** نقل تيارات الفيديو والصوت الخام (Raw Media Streams) المباشرة بين المستخدمين.
*   **السبب:** تقليل الحمل على الخادم وتوفير زمن انتقال منخفض جداً للصورة.

### المسار الثاني: الخادم المركزي (Socket.IO)
*   **المهمة:** استلام النصوص، ترجمتها، وتحويلها لصوت (TTS)، ثم إرسالها.
*   **نمط الترجمة (Fan-out Translation):**
    1. يلتقط المتصفح النص بلغة المتحدث.
    2. يُرسل النص (Raw Transcript) للخادم.
    3. الخادم يتصل بـ Groq لترجمة النص لجميع المتصلين (حسب لغة كل منهم).
    4. الخادم يتصل بـ ElevenLabs لتوليد الصوت باللغة الهدف.
    5. الخادم يُرسل حدث `translated-audio` (مرفق به الصوت كـ Base64) لكل مستخدم بشكل مخصص.

---

## 3. حلول التداخل والاستقرار (Conflict Resolutions)

*   **تأخير الضيف (The 1200ms Delay Trick):** 
    لمنع تعارض وصول الكاميرا/الصوت لـ WebRTC مع خدمة التعرف على الصوت (Web Speech API)، يقوم המضيف ببدء الاستماع فوراً، بينما ينتظر الضيف `1200ms` حتى ينتهي تفاوض WebRTC، مما يمنع تعليق الميكروفون.
*   **الاتصال المزدوج (Full-Duplex):**
    تم فصل حالة واجهة المستخدم عن التشغيل الفعلي للميكروفون باستخدام مرجع `shouldListenRef`. عندما يعمل الصوت المترجم (TTS)، تتغير واجهة المستخدم إلى "يتحدث..." لكن الميكروفون يظل يستمع في الخلفية، مما يسمح بالتحدث والمقاطعة الطبيعية دون أن يتوقف الميكروفون.
*   **منع الصدى (Echo Cancellation):**
    في واجهة المستخدم، يتم رفض عرض أو نطق النصوص المترجمة إذا كان `speakerId` يطابق معرف المستخدم نفسه، لكي لا يسمع المستخدم صدى لكلامه.

---

## 4. خريطة الطريق والأفكار المستقبلية (Roadmap & Future Ideas)
تم اقتراح عدة تحسينات لرفع جودة وموثوقية التطبيق:

1.  **سجل المحادثة النصي (Transcription History):**
    إضافة لوحة جانبية (Side Panel) تحتفظ بسجل كامل لكل الجمل التي قيلت وترجمتها (شات للترجمة)، ليتمكن المستخدم من قراءتها إذا فاته الصوت المترجم، مع إمكانية تصدير السجل في نهاية الاجتماع.
2.  **التعامل مع انقطاع الخدمة (Fallback Mechanisms):**
    في حال نفاد رصيد API لـ (ElevenLabs أو Groq) أو فشل الخادم، يعود المتصفح تلقائياً لاستخدام محرك الترجمة ونطق الصوت المدمج في المتصفح `window.speechSynthesis` لضمان عدم توقف الخدمة.
3.  **تحسينات واجهة المستخدم (UX Polish):**
    *   زر "كتم صوت المترجم" للمستخدمين الذين يفضلون القراءة فقط من الشاشة وسماع صوت المتحدث الأصلي.
    *   مؤشر استقرار الاتصال (Ping/Latency Indicator) لتنبيه المستخدم إذا كان اتصاله يؤثر على سرعة الترجمة.
4.  **معالجة النتائج الأولية (Interim Results Optimization):**
    إرسال النصوص غير المكتملة (Interim) عبر Socket.IO لعرضها فوراً على شاشة الطرف الآخر أثناء تحدث المتكلم وقبل أن ينهي جملته. وفقط عندما ينهي الجملة (`isFinal`)، يتم توليد الصوت ونطقه. هذا سيجعل الاستجابة تبدو أسرع بكثير على الشاشة.
